package com.payment.common.enums;

public enum TransactionStatus {
    INITIATED,
    PENDING,
    AUTHORIZED,
    CAPTURED,
    SETTLED,
    DECLINED,
    FAILED,
    REVERSED,
    REFUNDED,
    CANCELLED,
    EXPIRED
}
